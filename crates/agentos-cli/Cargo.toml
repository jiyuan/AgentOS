[package]
name = "agentos-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
agentos-core.workspace = true
agentos-interfaces.workspace = true
agentos-llm.workspace = true
agentos-memory-vector.workspace = true
agentos-proto.workspace = true
async-trait.workspace = true
getrandom.workspace = true
serde_json.workspace = true
tokio.workspace = true
toml.workspace = true
tracing-subscriber.workspace = true

[dev-dependencies]
libc = "0.2"
# The gateway's admission ordering (`AF-002`, `AF-004`) is tested against a real
# ledger: rusqlite installs the write failpoint, tokio-util builds the process
# cancellation root the receive loop selects on.
rusqlite.workspace = true
tokio-util.workspace = true
